https://toronto-micro-atlas.pages.dev/*
  X-Robots-Tag: noindex, nofollow

# The pages.dev subdomain stays out of search indexes so it never competes
# with the production domain (torontomicroatlas.com), which is indexable.
